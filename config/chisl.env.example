# Copy to your appdata mount as:
#   /mnt/user/appdata/opencode/.config/opencode/chisl.env
# chmod 600 that file. Restart the container after editing.
#
# Use this when Unraid template variables are not injected into the container
# (common after adding new template fields to an existing install).

AIONCORE_URL=http://192.168.0.68:64921/
AIONCORE_TOKEN=PASTE_TOKEN_FROM_CHISL_INSTALL_PLUGIN
